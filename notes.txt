minikube start --mount=true --mount-string=/Users/peterlarsson/src/ticketing/coolstorage:/host/data --driver=virtualbox


minikube start --vm=true

  // [x: string]: any;


error in route handler

error outside route handler

nats connection goes down after a bunch of retries

db connection is lost

uncaught exception stops node but process does not exit meaning kubernetes does not restart pod.

import { mocked } from 'ts-jest/utils';
const mockedSocketWrapper = mocked(SocketWrapper, true);


docker run -p 5432:5432 --name test-db -e POSTGRES_PASSWORD=1 postgres



import { mocked } from 'ts-jest/utils';

jest.mock('../../../socket/socket-wrapper');
const mockedSocketWrapper = mocked(SocketWrapper, true);


